---
import { getCollection } from "astro:content"
import Projects from "../../Components/Projects.astro"

const artProjects = (await getCollection("works"))
    .filter(project => project.data.type=='design')
---

<Projects title="Design" projects={artProjects}>
</Projects>